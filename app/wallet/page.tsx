import { Wallet } from "@/components/Wallet";

export default function WalletPage() {
  return (
    <div className="min-h-screen bg-[hsl(var(--background))] p-3 relative z-0">
      <Wallet />
    </div>
  );
}
